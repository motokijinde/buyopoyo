{
  "name": "buyopoyo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "ぶよぽよ - 落ち物パズル（iPhone向けWebアプリ）",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "pixi.js": "^8.6.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
